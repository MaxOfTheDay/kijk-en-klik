{
  "name": "kijk-en-klik",
  "version": "1.0.0",
  "private": true,
  "description": "A photo scavenger hunt PWA for family walks.",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
